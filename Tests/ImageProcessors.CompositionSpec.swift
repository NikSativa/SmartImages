import Nimble
import NSpry
import NSpry_Nimble
import Quick

@testable import NImageDownloader

final class ImageProcessors_CompositionSpec: QuickSpec {
    override func spec() {
        describe("ImageProcessors.Composition") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
