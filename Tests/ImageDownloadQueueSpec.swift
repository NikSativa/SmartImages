import Nimble
import NSpry
import NSpry_Nimble
import Quick

@testable import NImageDownloader

final class ImageDownloadQueueSpec: QuickSpec {
    override func spec() {
        describe("ImageDownloadQueue") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
